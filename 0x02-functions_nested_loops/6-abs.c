#include "main.h"

/**
 * _abs - computes the avsolute value of an integer
 * @n: variable used.
 * Return: absolute value if int.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

