#include "main.h"

/**
 * mul - multiplies a and b
 * @a: first variable used
 * @b: second variable used.
 * Return: the result, i.e c
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
